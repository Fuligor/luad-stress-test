[project]
name = "conference-results-pipeline"
version = "0.1.0"
description = "Pipeline for evaluating model robustness, generating bootstrap tables, and charting."
readme = "README.md"
requires-python = ">=3.9"

dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.2.0",
    "tqdm>=4.65.0",
    "pyarrow>=12.0.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0"
]